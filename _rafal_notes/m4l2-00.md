Super. To jest dokładnie to, co chciałbym zobaczyć.

Masz tylko:

```text
.claude/.10x-cli-manifest.json
.claude/prompts/
```

czyli efekty:

```bash
npx @przeprogramowani/10x-cli@latest get m4l2
```

Nie dotknęliśmy jeszcze kodu tldraw.

---

Ja bym teraz zrobił pierwszy commit startowy.

```bash
git add .claude
git commit -m "chore(m4l2): add project mapping prompts"
```

Dlaczego?

Bo później historia będzie wyglądała ładnie:

```text
commit 1
↓
przygotowanie warsztatu

commit 2
↓
artifact-1 territory

commit 3
↓
artifact-2 structure

commit 4
↓
artifact-3 contributors

commit 5
↓
repo-map synthesis
```

To może się przydać przy certyfikacji, bo pokaże naturalny proces pracy.

---

Po tym commicie NIE zaczynałbym jeszcze od prompta z lekcji.

Zrobiłbym 5-minutowy reconnaissance.

W Claude Code:

```text
Before we start generating any artifacts:

Briefly inspect this repository and answer:

1. What is the primary technology stack?
2. Is this a monorepo?
3. Which package manager is used?
4. Roughly how many packages/apps are present?
5. Can dependency-cruiser be used directly or will it require additional configuration?

Do not create files.
Do not generate artifacts.
Keep answer concise.
```

To nie jest część lekcji.

To jest coś, co zrobiłby architekt w prawdziwej pracy zanim zacznie analizować repo.

Po tym wyniku będziemy wiedzieli:

```text
jak wygląda tldraw
jak duży jest
gdzie później uruchomimy dependency-cruiser
czy są jakieś pułapki monorepo
```

## ODP:
```
 1. Tech stack — TypeScript + React. This is the tldraw infinite-canvas SDK monorepo, with Vitest for tests, oxlint/oxfmt for code quality, and Cloudflare Workers for the  
  dotcom backend.
  2. Monorepo? — Yes. Yarn workspaces covering packages/*, apps/*, apps/vscode/*, apps/dotcom/*, internal/*, and templates/*.
  3. Package manager — Yarn 4 (yarn@4.12.0, via Corepack). Not npm.
  4. Package/app count — ~17 SDK packages, ~9 apps (some with nested workspaces), ~12 templates, plus internal/* — roughly 40–50 workspaces total.
  5. dependency-cruiser — Not usable directly; it's not installed and there's no .dependency-cruiser.* config. It would need:
    - Installation (or yarn dlx dependency-cruiser)
    - A config with tsConfig pointing at the repo's tsconfig so @tldraw/* path aliases and project references resolve
    - Scoping (e.g. per-package runs or --include-only) — cruising all ~50 workspaces at once would be slow and noisy
```

i dopiero wtedy odpalimy właściwe:

```text
m4l2-1-territory-git-history
```

czyli pierwszy artefakt M4L2.


--- ML4-1 - Territory

Check the existing circular dependency script first.

Look at:
- package.json
- internal/scripts/check-circular-deps.ts

Tell me only:
1. Does it use dependency-cruiser?
2. What folders does it check?
3. Should we reuse it for M4L2 artifact-2?

Do not create files.

--- ML4-2 - Structure

Use the prompt from:

.claude/prompts/m4l2-2-structure-dependency-cruiser.md

Read:
context/map/artifact-1-territory.md

Create:
context/map/artifact-2-structure.md

Focus on:
- packages/editor/src
- packages/tldraw/src
- apps/dotcom/client/src/tla

Do not generate SVG or visual graphs.

I only want markdown findings:
- dependency hubs
- cycles
- architectural boundaries
- blast radius
- testability risks

Save the result to:
context/map/artifact-2-structure.md

--- ML4-3 - contributors

Use the prompt from:

.claude/prompts/m4l2-3-contributors-git.md

Read first:
- context/map/artifact-1-territory.md
- context/map/artifact-2-structure.md

Create:
context/map/artifact-3-contributors.md

Focus on:

1. Who appears to own or understand:
   - packages/editor/src
   - packages/tldraw/src
   - apps/dotcom/client/src/tla

2. Which contributors repeatedly appear in:
   - Editor.ts
   - core editor architecture
   - shape utilities
   - dotcom application

3. Identify:
   - domain experts
   - architectural owners
   - areas with concentrated knowledge
   - potential knowledge silos

Important:
- Prefer evidence from git history.
- Distinguish facts from assumptions.
- Do not generate repo-map.md yet.

Save result to:
context/map/artifact-3-contributors.md

--- M4L2-4 Roadmap

Use the prompt from:

.claude/prompts/m4l2-repo-map-synthesis.md

Read and synthesize:

- context/map/artifact-1-territory.md
- context/map/artifact-2-structure.md
- context/map/artifact-3-contributors.md

Create:

context/map/repo-map.md

Requirements:

1. Produce a concise onboarding-oriented project map.
2. Focus on decision-making value, not exhaustive details.
3. Highlight:
   - active areas
   - architectural boundaries
   - key dependency hubs
   - blast radius risks
   - knowledge concentration risks
   - recommended starting points for new contributors
4. Include:
   - evidence-backed findings
   - known unknowns
   - areas that need deeper investigation
5. Do not repeat large tables from source artifacts.
6. Prefer synthesis over summarization.
7. Keep the document practical for an architect onboarding to the repository.

Output:
- Save only to context/map/repo-map.md
- Do not modify existing artifacts
